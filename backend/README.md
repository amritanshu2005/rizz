# Backend (Express)

This is a minimal Express server exposing `/generate` and `/generate-opener`.

Configure `.env` (copy from `.env.example`) and set `GEMINI_API_KEY` if you want real AI calls.

Start:

```
cd backend
npm install
npm run dev
```
